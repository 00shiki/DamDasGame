import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    Circle: {
        flex: 1,
        backgroundColor: '#000',
        borderRadius: 20,
    },
})

export default styles;